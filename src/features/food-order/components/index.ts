/**
 * Food Order Feature Components
 * Internal UI components (not part of public API).
 * These components are shared within the food-order feature only.
 *
 * Example structure (to be expanded as needed):
 * - MenuItem.tsx — Displays a single menu item
 * - MenuList.tsx — Lists all menu items
 * - CartItem.tsx — Displays a cart item
 * - CartSummary.tsx — Shows order total and item count
 */

// Components will be exported here as they are created
